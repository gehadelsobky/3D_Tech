# ============================================================
#  3D Tech — Environment Variables
#  Copy this file:  cp .env.example .env
#  Then edit with your production values
# ============================================================

# Server
PORT=3001
NODE_ENV=production

# Security — CHANGE THIS! Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=

# Public site URL — REQUIRED in production for password reset emails.
# Reset links are built from this, never from the request's Host header
# (that header is attacker-controlled). No trailing slash.
# Example: PUBLIC_URL=https://3dtecheg.com
PUBLIC_URL=

# CORS — comma-separated allowed origins (leave empty to allow all)
# Example: CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
CORS_ORIGINS=

# Email (SMTP) — for form submission notifications
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
NOTIFY_EMAIL=
